コマンドエディタ仕様
====================

ベースは README.md の UI仕様 を参照。

## コマンドエディタ固有のUI仕様

引数は可変であるため、引数の追加・削除の操作が可能なUIとする。

UIレイアウトは以下を参照すること。

1. **全体像**: `appendix/command-editor.jpg` を参照。
2. **戻り値のデータ型のUI**: `appendix/return-type-edit.jpg` を参照。
3. **引数のリストのUI**: `appendix/argument-list-edit.jpg` を参照。
    - **引数の編集**: モーダルダイアログ。 `appendix/argument-edit.jpg` を参照。

