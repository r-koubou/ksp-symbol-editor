UIデータ型エディタ仕様
====================

ベースは README.md の UI仕様 を参照。

## UIデータ型エディタ固有のUI仕様

- 引数は可変であるため、引数の追加・削除の操作が可能なUIとする。

UIレイアウトは以下を参照すること。

1. **全体像**: `appendix/ui-type-editor.jpg` を参照。
2. **引数のリストのUI**: `appendix/argument-list-edit.jpg` を参照。
    - **引数の編集**: モーダルダイアログ。 引数の編集モーダルダイアログのUIは、コールバックエディタの引数の編集モーダルダイアログのUIと同様とし、`appendix/callback-argument-edit.jpg` を参照。

