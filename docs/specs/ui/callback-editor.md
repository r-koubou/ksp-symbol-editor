コールバックエディタ仕様
====================

ベースは README.md の UI仕様 を参照。

## コールバックエディタ固有のUI仕様

- 引数は可変であるため、引数の追加・削除の操作が可能なUIとする。
- コマンドエディタとは異なり、引数のデータ型の選択の要素は不要。

UIレイアウトは以下を参照すること。

1. **全体像**: `appendix/callback-editor.jpg` を参照。
2. **引数のリストのUI**: `appendix/argument-list-edit.jpg` を参照。
    - **引数の編集**: モーダルダイアログ。 `appendix/argument-edit.jpg` を参照。

